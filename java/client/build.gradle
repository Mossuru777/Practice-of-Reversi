dependencies {
    compile 'io.socket:socket.io-client:0.9.0' // 1.0.0だとnamespaceでプロトコル不整合が発生する
}

mainClassName = 'net.mossan.java.reversi.client.ReversiClient'

jar {
    manifest {
        attributes 'Main-Class': 'net.mossan.java.reversi.client.ReversiClient'
    }
}
